import Filter from "./Filter";

export default function FilterCompanyContainer() {
  const hasFeature = true;
  const hasAbility = true;

  return <Filter hasFeature={hasFeature} hasAbility={hasAbility} />;
}
